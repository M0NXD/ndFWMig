# Firewall Migration Tool has no third-party runtime dependencies.
# It uses only the Python standard library (tkinter for the GUI,
# xml.etree.ElementTree for PAN-OS XML).
#
# Development/testing only:
pytest>=7.0
